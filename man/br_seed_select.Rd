% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09-seed.R
\name{br_seed_select}
\alias{br_seed_select}
\title{Select genes via Lasso -> multivariate -> stepwise regression}
\usage{
br_seed_select(
  data,
  y,
  y_type,
  genes,
  x2 = NULL,
  p_threshold = 0.05,
  lasso_lambda = c("lambda.min", "lambda.1se"),
  lasso_nfolds = 10,
  step_direction = c("both", "backward", "forward"),
  step_k = 2,
  seed = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} containing clinical variables and gene expression
columns side by side. Gene columns must be numeric.}

\item{y}{Character vector specifying clinical indicators. For survival: a
length-2 vector \code{c("time", "status")}. For binary/ordinal: a single column
name. When screening against multiple indicators, pass a named list, e.g.,
\code{list(OS = c("time", "status"), ORR = "response")}.}

\item{y_type}{Character: \code{"survival"} or \code{"binary"}. Ordinal indicators are
not supported for selection/modeling steps.}

\item{genes}{Character vector of gene names (typically from
\code{\link[=br_seed_screen]{br_seed_screen()}}), or a \code{breg_seed_screen} object from which genes are
extracted automatically.}

\item{x2}{Optional character vector of adjustment covariates included in all
models.}

\item{p_threshold}{Numeric significance threshold for gene filtering (0 to 1).
Default is \code{0.05}.}

\item{lasso_lambda}{Lambda selection criterion: \code{"lambda.min"} (default) or
\code{"lambda.1se"}.}

\item{lasso_nfolds}{Number of folds for cross-validation in Lasso. Default 10.}

\item{step_direction}{Direction for stepwise selection: \code{"both"} (default),
\code{"backward"}, or \code{"forward"}.}

\item{step_k}{The penalty multiplier for AIC in stepwise selection. Default 2
(standard AIC).}

\item{seed}{Integer seed for reproducibility of Lasso cross-validation.}
}
\value{
A list with class \code{breg_seed_select} containing:
\itemize{
\item \code{lasso_genes}, \code{multivariate_genes}, \code{stepwise_genes}: gene sets from each stage
\item \code{final_genes}: intersection of all three gene sets
\item \code{lasso_model}, \code{multivariate_model}, \code{stepwise_model}: fitted model objects
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Step 3 of the SEED pipeline: refines the gene set through three sequential
stages --- Lasso regularization, multivariate regression, and stepwise
selection. The final gene set is the intersection of genes retained by all
three methods.
}
\examples{
\donttest{
set.seed(123)
n <- 100
test_data <- data.frame(
  time = rexp(n, 0.1),
  status = sample(0:1, n, replace = TRUE),
  GENE1 = rnorm(n),
  GENE2 = rnorm(n, mean = 0.8 * (1:n) / n),
  GENE3 = rnorm(n, mean = -0.5 * (1:n) / n),
  GENE4 = rnorm(n),
  GENE5 = rnorm(n),
  age = rnorm(n, 60, 10)
)

if (rlang::is_installed("glmnet")) {
  sel <- br_seed_select(test_data,
    y = c("time", "status"),
    y_type = "survival",
    genes = c("GENE1", "GENE2", "GENE3", "GENE4", "GENE5"),
    x2 = "age",
    seed = 42
  )
  print(sel)
}
}
}
\seealso{
Other br_seed: 
\code{\link{br_seed}()},
\code{\link{br_seed_model}()},
\code{\link{br_seed_screen}()}
}
\concept{br_seed}
