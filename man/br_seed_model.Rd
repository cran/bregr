% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09-seed.R
\name{br_seed_model}
\alias{br_seed_model}
\title{Build a risk score model from selected genes}
\usage{
br_seed_model(
  data,
  y,
  y_type,
  genes,
  x2 = NULL,
  risk_scale = c("none", "zscore")
)
}
\arguments{
\item{data}{A \code{data.frame} containing clinical variables and gene expression
columns side by side. Gene columns must be numeric.}

\item{y}{Character vector specifying clinical indicators. For survival: a
length-2 vector \code{c("time", "status")}. For binary/ordinal: a single column
name. When screening against multiple indicators, pass a named list, e.g.,
\code{list(OS = c("time", "status"), ORR = "response")}.}

\item{y_type}{Character: \code{"survival"} or \code{"binary"}. Ordinal indicators are
not supported for selection/modeling steps.}

\item{genes}{Character vector of final gene names, or a \code{breg_seed_select}
object from which \code{final_genes} are extracted.}

\item{x2}{Optional character vector of adjustment covariates included in all
models.}

\item{risk_scale}{How to scale expression values before computing risk score.
\code{"none"} (default) uses raw expression; \code{"zscore"} centers and scales.}
}
\value{
A list with class \code{breg_seed_model} containing:
\itemize{
\item \code{model}: \code{breg} object with the final model
\item \code{coefficients}: data.frame of gene coefficients
\item \code{risk_score}: numeric vector, one per sample
\item \code{risk_score_formula}: character string representation of the risk formula
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Step 4 of the SEED pipeline: builds a final regression model from the
selected gene set and computes a risk score for each sample. The risk score
is the linear predictor (gene expression  x  coefficient) summed across genes.
}
\examples{
\donttest{
set.seed(123)
n <- 100
test_data <- data.frame(
  time = rexp(n, 0.1),
  status = sample(0:1, n, replace = TRUE),
  GENE1 = rnorm(n),
  GENE2 = rnorm(n, mean = 0.8 * (1:n) / n),
  GENE3 = rnorm(n, mean = -0.5 * (1:n) / n),
  age = rnorm(n, 60, 10)
)

mod <- br_seed_model(test_data,
  y = c("time", "status"),
  y_type = "survival",
  genes = c("GENE1", "GENE2", "GENE3"),
  x2 = "age"
)
print(mod)
}
}
\seealso{
Other br_seed: 
\code{\link{br_seed}()},
\code{\link{br_seed_screen}()},
\code{\link{br_seed_select}()}
}
\concept{br_seed}
