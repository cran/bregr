% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09-seed.R
\name{br_seed_screen}
\alias{br_seed_screen}
\title{Screen genes via univariate regression}
\usage{
br_seed_screen(
  data,
  y,
  y_type,
  genes = NULL,
  x2 = NULL,
  p_threshold = 0.05,
  n_workers = 1L,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} containing clinical variables and gene expression
columns side by side. Gene columns must be numeric.}

\item{y}{Character vector specifying clinical indicators. For survival: a
length-2 vector \code{c("time", "status")}. For binary/ordinal: a single column
name. When screening against multiple indicators, pass a named list, e.g.,
\code{list(OS = c("time", "status"), ORR = "response")}.}

\item{y_type}{Character vector of indicator types: \code{"survival"}, \code{"binary"},
or \code{"ordinal"}. Must match \code{y} in length.}

\item{genes}{Character vector of gene column names. If \code{NULL}, all numeric
columns not in \code{y} or \code{x2} are used.}

\item{x2}{Optional character vector of adjustment covariates included in all
models.}

\item{p_threshold}{Numeric significance threshold for gene filtering (0 to 1).
Default is \code{0.05}.}

\item{n_workers}{Integer number of parallel workers for batch regression.
Passed to \code{\link[=br_pipeline]{br_pipeline()}}.}

\item{...}{Additional arguments passed to \code{\link[=br_pipeline]{br_pipeline()}}.}
}
\value{
A list with class \code{breg_seed_screen} containing:
\itemize{
\item \code{results}: data.frame of screening results (gene, indicator, p.value, etc.)
\item \code{genes}: character vector of significant genes (intersection across indicators)
\item \code{breg}: the underlying \code{breg} object (for regression-based screening)
\item \code{n_genes_in}, \code{n_genes_out}: counts for progress reporting
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Step 1 of the SEED pipeline: runs univariate regression for each gene against
one or more clinical indicators. Supports Cox regression (survival), logistic
regression (binary), and Spearman correlation (ordinal). When multiple
indicators are provided, genes significant across \emph{all} indicators are
retained (intersection).
}
\examples{
\donttest{
set.seed(123)
n <- 100
# Create gene with real survival signal
test_data <- data.frame(
  time = rexp(n, 0.1),
  status = sample(0:1, n, replace = TRUE),
  GENE1 = rnorm(n),
  GENE2 = rnorm(n, mean = 0.8 * (1:n) / n),
  GENE3 = rnorm(n),
  age = rnorm(n, 60, 10)
)

# Single survival indicator; use relaxed p_threshold for example data
res <- br_seed_screen(test_data,
  y = c("time", "status"),
  y_type = "survival",
  genes = c("GENE1", "GENE2", "GENE3"),
  x2 = "age",
  p_threshold = 0.5
)
print(res)
}
}
\seealso{
Other br_seed: 
\code{\link{br_seed}()},
\code{\link{br_seed_model}()},
\code{\link{br_seed_select}()}
}
\concept{br_seed}
